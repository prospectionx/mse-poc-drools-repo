{
  "name": "root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "pull-drools": "cross-env NODE_ENV=development dotenv-flow -p config -- node ./scripts/sync-drools pull",
    "push-drools": "cross-env NODE_ENV=development dotenv-flow -p config -- node ./scripts/sync-drools push",
    "clean-drools": "cross-env NODE_ENV=development dotenv-flow -p config -- node ./scripts/sync-drools clean"
  },
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "@types/node": "16.11.45",
    "esbuild": "0.14.49",
    "esbuild-register": "3.3.3",
    "isomorphic-git": "1.19.0",
    "cpy": "9.0.1",
    "del": "6.1.1",
    "eol": "0.9.1"
  },
  "devDependencies": {
    "cross-env": "7.0.3",
    "dotenv-flow-cli": "1.0.0",
    "eslint": "8.20.0",
    "typescript": "4.7.4"
  }
}
